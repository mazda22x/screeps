export const Bodies = {
  defaultWorker: [WORK, CARRY, MOVE]
};

export enum ACTIONS {
  MOVE = 'moveTo',
  HARVEST = 'harvest',
  BUILD = 'build',
  UPGRADE = 'upgradeController',
  TRANSFER = 'transfer',
}
